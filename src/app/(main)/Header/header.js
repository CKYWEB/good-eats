export const Logo = () => {
    // TODO: below is just an example, you need to rewrite this component
    return (
      <div>
        Logo
      </div>
    );
};

export const Avatar = () => {
    // TODO: below is just an example, you need to rewrite this component
    return (
      <div>
        Avatar
      </div>
    );
};

export const Header = () => {
    return (
        // TODO: below is just an example, you need to rewrite this component
      <div className="border border-black d-flex justify-content-between">
        <Logo />
        This is the Header
        <Avatar />
      </div>
    );
};