export default function Home () {
    return (
        // TODO: below is just an example, you need to rewrite this component
      <h1>
        This is the Home body, nested in between header and footer
      </h1>
    );
}